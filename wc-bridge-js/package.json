{
  "name": "wc-bridge",
  "version": "0.1.0",
  "description": "Wraps React and Angular components as custom elements following the same attribute/event/style contract used by the leptos-webcomponent crate.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=esm --platform=neutral --outfile=dist/index.js",
    "test": "vitest run"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "vitest": "^1.3.0"
  }
}
